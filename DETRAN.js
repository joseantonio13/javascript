
var vel = 70
var dist = 2 

console.log ( `a distancia é de  ${dist}  e a velocidade é de: ${vel}`);

/* sempre usamos o acento do é, segurando o shift e apertando duas vezes o acento assim: `` para dizer uma coisa no console e usando Cifrão ${ nome da variavel } para aparecer a variavél junto */

if (vel >= 70) {
    console.log(`Que Velocidade é essa Junior`)
}

/* Acabamos de criar uma condição simples (simples pois não tem else/se não) com javascript */

